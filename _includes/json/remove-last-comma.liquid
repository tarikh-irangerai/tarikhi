{%-comment-%}
zamtar tarah
{%-endcomment-%}

{%-comment-%} out: trim_json_out {%-endcomment-%}

{%- if include.json != empty and include.json != nil -%}
  {%-comment-%} remove last comma {%-endcomment-%}
  {%- assign _json = include.json | append: '@_' | remove_first: ',@_' -%}
  {%- capture trim_json_out -%}
  [
    {{ _json }}
  ]
  {%- endcapture -%}
{%- endif -%}

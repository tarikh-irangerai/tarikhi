

{%-comment-%} out: page_title {%-endcomment-%}

{%- assign current_lng = lng %}
{%- if lng == nil -%} {%- assign current_lng = get_lng -%} {%- endif -%}

{%- assign page_title = include.title | replace: "_autogeneratedsamplecontent_", site.data.lang[current_lng].auto_content.title -%}
